// logs — GET /api/v1/projects/[key]/logs?cursor=: one stateless window over
// the project's dated log; the echoed cursor carries the resume position.
// Mutating verbs refused.
export * from "./route";
