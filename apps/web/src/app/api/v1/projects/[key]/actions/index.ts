// actions — POST /api/v1/projects/[key]/actions: start/stop/restart with the
// TUI's guards (no starting a disabled project; install-then-start only when
// the job is no longer registered). GET refused.
export * from "./route";
