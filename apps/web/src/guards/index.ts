/**
 * Package-boundary guards: proof tests that hold @score/web to its shape —
 * the loopback-only command contract and the removal gate for the deleted
 * server stub. Owns verification only; no runtime module lives here, so
 * there is nothing to re-export — the front door exists to state that.
 */
