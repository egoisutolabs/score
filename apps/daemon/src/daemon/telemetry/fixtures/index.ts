/**
 * Test fixtures for the daemon telemetry subfeature: the #78
 * result-to-telemetry mapping table.
 * Test support only — production code never imports from here.
 */
export * from "./telemetry.fixture";
