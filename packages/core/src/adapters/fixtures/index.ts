/**
 * Test fixtures for the adapters feature: the staged-merge residue repo
 * builder (#92) and the real-subprocess runner it needs. Test support only —
 * production code never imports from here.
 */
export * from "./staged-merge.fixture";
