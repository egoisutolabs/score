/**
 * Maintenance: the legacy cleanup-then-dispatch composition run as one
 * tick. Thin; exists to preserve the legacy pass shape.
 */
export * from "./maintenance.render";
export * from "./maintenance.service";
