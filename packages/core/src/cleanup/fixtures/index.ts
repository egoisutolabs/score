/**
 * Test fixtures for the cleanup feature: the merged-PR fakes and the
 * stranded-issue (#64) workspace/agent fakes. Test support only —
 * production code never imports from here.
 */
export * from "./cleanup.fixture";
